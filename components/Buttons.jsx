const PrimaryButton = () => {
    return (
        <button>
            PrimaryButton
        </button>
    );
}

const SecondaryButton = () => {
    return (
        <button>
            SecondaryButton
        </button>
    );
}

export default {PrimaryButton, SecondaryButton}